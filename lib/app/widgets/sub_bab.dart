import 'package:book/app/widgets/heading_1.dart';
import 'package:flutter/material.dart';

class ATujuan extends StatelessWidget {
  const ATujuan({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Heading1(numbering: "A", title: "Tujuan Pembelajaran");
  }
}

class BKata extends StatelessWidget {
  const BKata({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "B", title: " Kata Kunci");
  }
}

class CInfo extends StatelessWidget {
  const CInfo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "C", title: " Infografis");
  }
}

class DAyo extends StatelessWidget {
  const DAyo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "D", title: " Ayo Tadarus");
  }
}

class ETadabb extends StatelessWidget {
  const ETadabb({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "E", title: "Tadabbur");
  }
}

class GWawasan extends StatelessWidget {
  const GWawasan({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "G", title: "Wawasan Keislaman");
  }
}

class FKisah extends StatelessWidget {
  const FKisah({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "F", title: "Kisah Inspiratif");
  }
}

class HPenerapan extends StatelessWidget {
  const HPenerapan({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "H", title: "Penerapan Karakter");
  }
}

class IReflek extends StatelessWidget {
  const IReflek({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "I", title: "Refleksi");
  }
}

class IPengaya extends StatelessWidget {
  const IPengaya({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "L", title: "Pengayaan");
  }
}

class KPenilai extends StatelessWidget {
  const KPenilai({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "K", title: "Penilaian");
  }
}

class JRangkum extends StatelessWidget {
  const JRangkum({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Heading1(numbering: "J", title: "Rangkuman");
  }
}
